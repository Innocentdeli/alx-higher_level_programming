#!/usr/bin/python3
if __name__ == __main__:
    '''Print the addition of all arguments'''
    count = len(sys.argv)
    total = 0
    for i in range(count):
        total += int(sys.argv[i]
    print(f"{total}")
